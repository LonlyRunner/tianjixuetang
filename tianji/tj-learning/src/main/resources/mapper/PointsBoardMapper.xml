<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianji.learning.mapper.PointsBoardMapper">

    <insert id="createPointsBoardTable" parameterType="java.lang.String">
        CREATE TABLE `${tableName}`
        (
            `id`      BIGINT NOT NULL AUTO_INCREMENT COMMENT '榜单id',
            `user_id` BIGINT NOT NULL COMMENT '学员id',
            `points`  INT    NOT NULL COMMENT '积分值',
            `rank`    INT    NOT NULL COMMENT '名次',
            PRIMARY KEY (`id`) USING BTREE,
            INDEX `idx_user_id` (`user_id`) USING BTREE,
            INDEX `idx_rank` (`rank`) USING BTREE
        )
            COMMENT ='学霸积分榜'
            COLLATE = 'utf8mb4_0900_ai_ci'
            ENGINE = InnoDB
            ROW_FORMAT = DYNAMIC
    </insert>

    <select id="queryHistoryBoardList" resultType="com.tianji.learning.domian.po.PointsBoard">
        SELECT user_id, points, `rank`
        FROM ${tableName}
        ORDER BY `rank` ASC
        LIMIT #{from}, #{pageSize}
    </select>

    <select id="queryMyHistoryBoard" resultType="com.tianji.learning.domian.po.PointsBoard">
        SELECT user_id, points, `rank`
        FROM ${tableName}
        WHERE user_id = #{userId}
        LIMIT 1
    </select>
</mapper>